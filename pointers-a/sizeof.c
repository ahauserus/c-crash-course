#include <stdio.h>

/**
 * main - Entry point of the program
 * Return: 0 is success, 1 otherwise
 */

int main(void)
{
	int *array[5];

	printf("Size of the array is: %ld byte(s)\n", sizeof(array));

	return (0);
}
