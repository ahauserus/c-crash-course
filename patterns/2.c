#include <stdio.h>

/**
 * main - Entry point. Prints pattern 2 of the README file
 * Return: 0 is success, 1 otherwise
 */

int main(void)
{
	int i, j, k;
	int rows = 7;

	for (i = 1; i <= rows; i++)
	{
		for (j = 1; j <= i; j++)
	}
	return (0);
}
